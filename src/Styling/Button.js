import { StyleSheet } from "react-native";

export const DEFAULT_BUTTON_STYLE = StyleSheet.create({
    container_button: {
      padding: 10,
    },
    button: {
      alignItems: "center",
    },
    textButton: {
      color: "white",
    },
  });
  